# 项目进展报告

> 生成时间：2026-09-11
> 仓库：https://github.com/JerryFreeman333/fdu-hackthon
> 本地副本：fdu-hackthon/（未 git 化）

---

## 一、项目概览

**项目名称**：复旦黑客松 2026 —— 老年健康 Agent
**核心目标**：做一个真正"认识这个老人"的健康助手，把对话、手表、血压、体检报告、用药和日常状态整合在一起看，重点是**发现变化**而非诊断疾病。
**当前阶段**：可本地运行的交互原型 + 规则引擎演示（路线一）

---

## 二、技术架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | React 18 + TypeScript | 纯前端原型，无后端依赖 |
| 构建工具 | Vite 5 | 快速热更新，适合演示 |
| 状态管理 | React useState / Context | 演示规模足够 |
| 数据持久化 | localStorage | 健康档案本地存储 |
| 代码质量 | TypeScript strict + Node test runner | 完整类型覆盖 |
| CI | npm scripts | format / typecheck / test / build / security check |

### 目录结构

```
fdu-hackthon/
├── docs/
│   └── 想法.md                          # 三条路线的原始脑暴记录
├── route1-health-agent/                 # 路线一主项目
│   ├── index.html / package.json / vite.config.ts / tsconfig*.json
│   ├── scripts/security-check.mjs
│   ├── src/
│   │   ├── main.tsx / App.tsx           # 入口 + 根组件
│   │   ├── types.ts                     # 全局类型定义（核心领域模型）
│   │   ├── styles.css
│   │   ├── data/
│   │   │   ├── demo.ts                  # 王秀兰奶奶 21 天演示数据
│   │   │   └── normalize.ts
│   │   ├── pipeline/events.ts           # 健康事件流抽象层
│   │   ├── store/
│   │   │   ├── HealthRecordStore.ts
│   │   │   └── LocalHealthData.ts       # localStorage 读写
│   │   ├── engine/                      # 核心业务引擎
│   │   │   ├── agent.ts                 # 对话 Agent + 意图识别
│   │   │   ├── detect.ts                # 检测引擎主入口
│   │   │   ├── baseline.ts              # 个人基线计算
│   │   │   ├── extract.ts               # 文本 → 结构化标签
│   │   │   ├── escalate.ts              # 家属通知分级逻辑
│   │   │   ├── report.ts                # 周报生成
│   │   │   ├── questions.ts             # 追问推荐
│   │   │   ├── context.ts / privacy.ts / tasks.ts / personTwin.ts
│   │   │   └── detection/
│   │   │       ├── types.ts / signals.ts / helpers.ts
│   │   │       ├── safety.ts            # 安全兜底（urgent）
│   │   │       ├── fusion.ts            # 多信号融合（核心）
│   │   │       └── rules/
│   │   │           ├── metricBaseline.ts
│   │   │           └── contextual.ts
│   │   ├── adapters/                    # 设备 / LLM / OCR 适配器
│   │   │   ├── DeviceAdapter.ts
│   │   │   ├── ImageHealthScanner.ts
│   │   │   └── DemoDeviceAdapter.ts
│   │   └── components/                  # React UI
│   │       ├── RoleGate.tsx / ElderHome.tsx
│   │       ├── ChatView.tsx / ProfileView.tsx
│   │       ├── FamilyView.tsx / FamilyDashboard.tsx
│   │       ├── ReportView.tsx / Sparkline.tsx
│   └── tests/
│       ├── detection.test.ts
│       ├── numeric-extract.test.ts
│       └── privacy-regression.test.ts
```

---

## 三、已完成功能详解

### 3.1 对话 Agent（src/engine/agent.ts）

**职责**：接收老人自由文本 → 识别症状标签 → 生成安全共情回复。

**意图识别规则**（13 类，全部基于正则）：

| 标签 | 触发关键词示例 | 回复策略 |
|------|---------------|---------|
| fatigue | 很累、乏、没劲 | 确认 + 结合活动量 |
| dyspnea | 喘、气短、上楼费劲 | 关注多信号融合 |
| poorSleep | 睡不好、失眠、夜醒 | 记录 + 追问频率 |
| edema | 脚肿、腿肿、鞋紧 | 提示就医 |
| dizziness | 头晕、站不稳 | 先坐稳、安全优先 |
| medicationMissed | 忘吃药、漏服 | 不自行加量 |
| bpHigh | 血压高、高压 140+ | 安静复测 |
| chestPain | 胸口痛、胸闷 | **立即急救**，不退让 |
| neuroChange | 嘴角歪、一侧无力、说话不清 | **立即急救**，不退让 |
| fall | 摔倒、跌了一跤 | 先别起身，标为 urgent |
| pain | 疼、痛 | 追问位置/时间 |
| moodLow | 心情不好、孤独、想孩子 | 共情倾听 |

**回复生成策略**：
- **Urgent 安全护栏**：检测到 urgent 级 Finding 且当前输入含 chestPain / neuroChange / fall → 优先输出急救指导
- **症状共情回复**：按标签匹配规则文案，短句温和
- **追问机制**：根据症状推荐 1 个追问问题（questions.ts）
- **多信号兜底**：触发 fusion 规则时自然引用最新证据
- **fall 特殊处理**：自动标 urgent，提示保持电话畅通

**适配器模式**：
- `ruleBasedAdapter`（默认）：纯规则，零网络依赖
- `createHttpLlmAdapter(url)`：接入真实 LLM（同源/HTTPS/localhost），API key 保服务端

### 3.2 个人基线引擎（src/engine/baseline.ts）

**核心理念**：不和"正常值"比，只和"这个老人自己平时"比。

```ts
computeBaseline(records, 'steps', { windowDays: 14, excludeDays: 3, minPoints: 5 })
deviationSigma(value, baseline, higherIsBad)   // 返回基线标准差的倍数
```

**关键设计**：
- 数据点 < 5 → 返回 null（沉默，不硬凑全局阈值）
- 排除最近 3 天恶化期，避免基线被污染
- 变差方向可配置（步数越低越坏 vs 静息心率越高越坏）

### 3.3 变化检测引擎（src/engine/detect.ts + detection/）

**主入口**：`runDetection(events, today)` → `Finding[]`

**检测流程**：
1. 健康事件流 → 数据物化层
2. 构建个人基线
3. 遍历规则（单指标 → 上下文 → 多信号融合 → 安全兜底）
4. 隐私过滤 → 去重 → 按严重度排序

**单指标基线规则**（detection/rules/metricBaseline.ts）：

| 指标 | 触发条件 | 阈值 | 严重度 |
|------|----------|------|--------|
| 步数 | 低于基线 | >30% 且连续 2 天 | watch |
| 步速 | 低于基线 | >20% | watch |
| 睡眠时长 | 低于基线 | >15% 且连续 2 天 | watch |
| 夜间醒来 | 高于基线 | >50% 且连续 2 天 | watch |
| 静息心率 | 高于基线 | >5 bpm | watch |
| 体重 | 高于基线 | >1.5 kg（心衰水肿） | alert |
| 血氧 | 低于基线 | >2% | alert |
| 收缩压 | 偏高 | 连续 2 天 >140 或较基线高 >20 | alert |

**上下文感知规则**（detection/rules/contextual.ts）：
- 连续 2 天同类主诉 → watch
- 连续 2 天活动量低于个人基线 → watch
- 连续 2 天血压偏高 → alert

**多信号融合规则**（detection/fusion.ts）—— **核心演示场景**：

```
活动维度:    步数↓ 或 步速↓
主诉维度:    fatigue 或 dyspnea
容量/睡眠:   体重↑ 或 水肿 或 夜醒↑
生理指标:    静息心率↑ 或 血氧↓
→ 同时满足 ≥ 3 个维度，触发 alert 级 Finding（fusion.multisignal_deterioration）
```

**安全兜底规则**（detection/safety.ts）：
- 跌倒 / 急性胸痛 / 神经症状 → **urgent**，立即急救
- 血压极端值 → alert
- urgent 不参与融合，单独走 escalation 通道

### 3.4 家属协同与分级通知（src/engine/escalate.ts）

**通知原则**：
- info / watch → 留在老人端，不打扰家属
- alert / urgent → 推送给家属，附证据链 + 处理路径

**授权控制**：
- `granted`：已授权，按分级推送
- `ask`：每次需要时征求老人同意
- `denied`：不推送

**推送消息结构**：
- 标题：建议关注 / 紧急
- 正文：evidence 每条一行（主观主诉 + 客观数据）
- 处理路径：carePath 文字
- 操作按钮：联系老人、查看完整档案

### 3.5 每周健康周报（src/engine/report.ts）

**生成逻辑**：
1. 取本周（weekEnd 往前 7 天）数据
2. 基线严格使用**本周开始之前**的数据（防数据泄露）
3. 每项指标：本周均值 vs 此前个人基线 → 计算百分比变化

**输出两个版本**：
- **老人版**：温和语言，正向引导；有变化时"我在帮您盯着"
- **家属版**：客观陈述变化；alert 存在时建议近期多联系老人

### 3.6 UI 组件层

| 组件 | 功能 |
|------|------|
| RoleGate.tsx | 老人 / 家属端角色切换 |
| ElderHome.tsx | 指标卡片 + 聊天入口 + 快速录入 + 周报入口 |
| ChatView.tsx | 对话历史 + 标签显示 + Agent 回复 + 快捷输入 |
| ProfileView.tsx | 健康档案（病史、用药、能力状态） |
| FamilyView.tsx | 通知列表 + 待观察列表 + 通知原则 |
| FamilyDashboard.tsx | 家属仪表盘 |
| ReportView.tsx | 周报查看页 |
| Sparkline.tsx | 21 天迷你折线图 |

---

## 四、演示场景

**角色**：王秀兰奶奶，72 岁，高血压 + 心功能减退随访中，使用拐杖。

**故事线**：
- 前 16 天（08-17 ~ 09-01）：活动约 5200 步/天，步速 0.88 m/s，睡眠 6.5h，体重 62.3 kg，稳定
- 最近 5 天（09-02 ~ 09-06）：逐步下滑

| 指标 | 09-01 | 09-06 | 变化 |
|------|-------|-------|------|
| 步数 | 5200 | 2800 | -46% |
| 步速 | 0.88 m/s | 0.68 m/s | -23% |
| 睡眠 | 6.5h | 5.4h | -17% |
| 夜醒 | 1 次 | 3 次 | +200% |
| 静息心率 | 72 bpm | 84 bpm | +12 bpm |
| 体重 | 62.3 kg | 64.3 kg | +2.0 kg |
| 血氧 | 96% | 94% | -2% |
| 主观 | — | "很累/喘/睡不好/脚肿" | — |

**预期触发**：单指标 watch × N → 连续活动低 watch → **多信号融合 alert** → 家属收到通知。

---

## 五、数据模型（src/types.ts）

```ts
// 10 类指标
MetricKey: steps | walkSpeed | sleepHours | nightWakes
          | restingHr | weight | spo2 | systolic | diastolic | bloodGlucose

// 12 类症状标签
SymptomTag: fatigue | dyspnea | poorSleep | edema | dizziness
           | medicationMissed | pain | moodLow | fall
           | bpHigh | chestPain | neuroChange

// 4 级严重度
Severity: info < watch < alert < urgent

// 老人档案
ElderProfile: { name, age, conditions, medications,
  mobility, nightVision, cognition, familySharing }

// 检测结果
Finding: { id, date, severity, title, detail,
  evidence[], familyMessage?, carePath?,
  ruleId, score, signalKeys[], familyEligible }

// 聊天
ChatMessage: { id, role: 'elder'|'agent', text, time, persisted? }

// 照护任务
CareTask: { id, title, dueDate, status, kind, sourceFindingId? }
```

---

## 六、测试覆盖

| 测试文件 | 覆盖内容 |
|----------|----------|
| detection.test.ts | 检测引擎核心 + 多信号融合 |
| numeric-extract.test.ts | 聊天文本 → 数值提取 |
| privacy-regression.test.ts | private 观察不推给家属 |

```bash
cd route1-health-agent
npm install
npm test
npm run ci  # format check → typecheck → test → build → security check
```

---

## 七、当前限制与下一步

### 已实现但需工程化

| 功能 | 当前 | 下一步 |
|------|------|--------|
| LLM 回复 | 纯规则 | 接入真实 LLM（createHttpLlmAdapter 已就绪） |
| 手表数据 | DemoDeviceAdapter 模拟 | 接入 HealthKit / Google Fit / 华为 |
| 拍照 OCR | 正则提取 | 接入阿里云 / 百度 OCR |
| 家属推送 | UI 展示 | 接入微信模板消息 / 短信 |
| 持久化 | localStorage | 后端 DB（Firebase / Supabase） |
| 账号体系 | 无 | 老人 / 家属账号 + 授权链路 |

### 尚未实现

- [ ] 老人主动拍照录入（ElderHome 入口已留，OCR 未完成）
- [ ] 体检报告拍照识别
- [ ] 用药提醒任务
- [ ] 社区医生对接路径
- [ ] 路线二（居家安全 3D 建模）
- [ ] 路线三（Person Twin + Home Twin 融合）

### 演示注意事项

- 演示数据均为模拟（src/data/demo.ts），**不可用于真实医疗判断**
- README 已声明：不做"万能养老平台"，不说 AI 能诊断疾病
- 隐私边界已设计但未做合规审计

---

## 八、运行指南

```bash
cd route1-health-agent
npm install
npm run dev
# http://localhost:5173
```

---

## 九、模块成熟度

| 模块 | 成熟度 | 说明 |
|------|--------|------|
| 检测引擎（单指标 + 多信号融合） | 5/5 | 完整，有测试，可演示 |
| Agent 意图识别 + 安全护栏 | 4/5 | 13 类规则完整，LLM 适配器已定义 |
| 分级家属通知 | 4/5 | 逻辑 + UI 完整，缺推送通道 |
| 个人基线计算 | 4/5 | 核心理念清晰，边缘 case 完整 |
| 每周健康周报 | 4/5 | 双版本，基线隔离逻辑正确 |
| UI 组件层 | 3/5 | 核心流程完整，细节打磨空间大 |
| 数据层 / 演示数据 | 3/5 | 演示场景完整，真设备接入未做 |
| OCR 拍照录入 | 2/5 | 接口已定义，集成未做 |
| 后端服务 | 1/5 | 未开始，纯前端原型 |

---

*本文档由代码分析自动生成，如有出入请以代码为准。*
